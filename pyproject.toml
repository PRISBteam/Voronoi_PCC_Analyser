[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "PCCanalyser"
version = "0.0.1"
dependencies = [
    "numpy", "scipy", "tqdm", "jupyterlab", 
    "matplotlib", "bokeh",
]